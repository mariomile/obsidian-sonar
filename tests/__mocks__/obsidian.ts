/**
 * Minimal `obsidian` stub for vitest. The `src/index/**` modules are pure and
 * must not pull `obsidian` at runtime; this alias only exists so that if a
 * transitive import ever appears, the test loader resolves to an empty module
 * instead of crashing. Deliberately sparse — add exports only if a test
 * genuinely needs one.
 */
export {};
